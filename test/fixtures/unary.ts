function invert(value: boolean): boolean {
  return !value;
}

function negate(n: number): number {
  return -n;
}

function getZero(): number {
  return 0;
}

function getOne(): number {
  return 1;
}

export { invert, negate, getZero, getOne };
